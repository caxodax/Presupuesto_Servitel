# Ambiente
NODE_ENV=development

# Configuración Base de Datos PostgreSQL (Ajustar credenciales)
DATABASE_URL="postgresql://user:password@localhost:5432/presupuestos_db?schema=public"

# Auth.js (NextAuth) Config
# Generar key real local mediante: npx auth secret
AUTH_SECRET="super-secret-key-development-only-123"
NEXTAUTH_URL="http://localhost:3000"
