# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: e2e/accounting-flow.spec.ts >> Flujo Contable Completo >> Test 2 & 3: Plan de cuentas visible y filtros
- Location: tests/e2e/accounting-flow.spec.ts:16:7

# Error details

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:3001/login
Call log:
  - navigating to "http://localhost:3001/login", waiting until "load"

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Flujo Contable Completo', () => {
  4  |   const userEmail = process.env.E2E_USER_EMAIL || 'fox@gmail.com';
  5  |   const userPassword = process.env.E2E_USER_PASSWORD || '123456'; // Ajustar según realidad
  6  | 
  7  |   test.beforeEach(async ({ page }) => {
  8  |     // 1. Login
> 9  |     await page.goto('/login');
     |                ^ Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:3001/login
  10 |     await page.fill('#email', userEmail);
  11 |     await page.fill('#password', userPassword);
  12 |     await page.click('button[type="submit"]');
  13 |     await expect(page).toHaveURL(/\/dashboard/);
  14 |   });
  15 | 
  16 |   test('Test 2 & 3: Plan de cuentas visible y filtros', async ({ page }) => {
  17 |     // 3. Ir a /dashboard/cuentas
  18 |     await page.goto('/dashboard/cuentas');
  19 |     await expect(page.locator('h1')).toContainText('Plan de Cuentas');
  20 |     
  21 |     // 4. Confirmar que se ve el árbol de cuentas (al menos las raíces 4, 5, 6)
  22 |     await expect(page.locator('text=4')).toBeVisible({ timeout: 10000 });
  23 |     await expect(page.locator('text=5')).toBeVisible();
  24 |     await expect(page.locator('text=6')).toBeVisible();
  25 | 
  26 |     // Filtros
  27 |     await page.fill('input[placeholder*="Buscar"]', 'INGRESOS');
  28 |     await expect(page.locator('text=INGRESOS')).toBeVisible();
  29 |   });
  30 | 
  31 |   test('Test 4: Activar/desactivar cuenta', async ({ page }) => {
  32 |     await page.goto('/dashboard/cuentas');
  33 |     // Buscamos una cuenta y probamos el toggle
  34 |     // Nota: Como activé todas vía script, probaremos desactivar una.
  35 |     const deactivateBtn = page.locator('button:has-text("Desactivar")').first();
  36 |     await expect(deactivateBtn).toBeVisible();
  37 |     await deactivateBtn.click();
  38 |     await expect(page.locator('text=Cuenta desactivada')).toBeVisible();
  39 |     await expect(page.locator('button:has-text("Activar")').first()).toBeVisible();
  40 |   });
  41 | 
  42 |   test('Test 6: Mapeo legacy', async ({ page }) => {
  43 |     await page.goto('/dashboard/cuentas/mapeo');
  44 |     await expect(page.locator('h1')).toContainText('Mapeo de Categorías');
  45 |     // Verificar que hay contenido (si hay categorías legacy)
  46 |     const table = page.locator('table');
  47 |     await expect(table).toBeVisible();
  48 |   });
  49 | 
  50 |   test('Test 14: Reportes - Pestaña Plan de Cuentas', async ({ page }) => {
  51 |     await page.goto('/dashboard/reportes');
  52 |     await page.click('button:has-text("Plan de Cuentas")');
  53 |     await expect(page.locator('text=Estado de Resultados Jerárquico')).toBeVisible();
  54 |     // Verificar que hay filas en la tabla
  55 |     await expect(page.locator('table tr').count()).toBeGreaterThan(1);
  56 |   });
  57 | });
  58 | 
```